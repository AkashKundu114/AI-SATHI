# Engineering Review Checklist

Use this file for a FAANG-style code and systems review. It names the core
invariants, what is covered by tests, and what a reviewer should inspect first.

## High-Level Claims

- Valid Meta webhooks are acknowledged quickly and processed asynchronously.
- Duplicate WhatsApp retries do not create duplicate ledger writes.
- User-visible financial numbers are computed by code, not generated by an LLM.
- Model/vendor failure degrades to bounded Bengali retry messages.
- Market advice cannot expose trends from fewer than the minimum seller sample.
- The active deployment path is Docker Compose plus Caddy on a single host.

## Code Paths to Review

| Area | Files |
|---|---|
| Webhook auth and queue handoff | `services/gateway/main.py` |
| WhatsApp parsing/sending/media | `shared/whatsapp/*` |
| State machine | `services/orchestrator/graph.py` |
| Model cascade | `services/orchestrator/model_router.py` |
| Ledger extraction/confirmation | `services/orchestrator/nodes/ledger_node.py`, `ledger_confirm_node.py` |
| Pricing and negotiation safety | `pricing_node.py`, `negotiation_node.py`, `price_chat_node.py`, `cross_verify.py` |
| Catalog image flow | `catalog_node.py`, `services/vision_service/*` |
| PDF report safety | `services/pdf_service/generator.py` |
| Market anonymity | `services/market_service/aggregator.py` |
| Runtime config | `shared/config/settings.py`, `.env.example`, `scripts/check_env.py` |

## Test Expectations

Run:

```bash
pytest tests/unit/ -q --tb=short
pytest tests/integration/ -q --tb=short
```

The unit and integration suites are offline and should not require API keys.
Fixtures live under `tests/fixtures/`; keep them small, synthetic, and free of
real user data.

Unit coverage includes:

- Webhook signature behavior.
- Bengali number helpers and calendar formatting.
- Ledger amount validation and confirmation flow.
- Pricing floor math.
- Negotiation floor and number-safety behavior.
- Catalog delivery fallbacks.
- Flux fallback behavior.
- Market trend classification.
- Grounding verifier behavior for scheme RAG components.
- Graph routing.

Integration coverage includes:

- Meta webhook HMAC verification, Redis deduplication, and queue handoff.
- WhatsApp webhook fixture parsing for text, audio, image, and interactive
  payload shapes.
- Gateway audio/image dispatch into STT, S3 upload, and Celery queue input.
- Orchestrator outbound delivery for text, document, and image messages.

## Production Readiness Bar

Before live users:

- No stale references to unused vendors in active docs.
- `make check-env` passes.
- Unit tests pass.
- Meta Business Verification and permanent token are complete.
- Sarvam spend cap is set.
- Redis/Postgres passwords are non-default.
- S3 bucket encryption and backups are configured.
- Incident owner knows how to inspect gateway and worker logs.

## Known Review Risks

- Government Scheme RAG is not part of active production routing.
- Single-host Docker Compose is acceptable for pilot/small deployment, but not
  a high-availability architecture.
- Local model fallback requires real capacity planning; setting
  `USE_LOCAL_MODELS=true` without a reachable Ollama host is not sufficient.
- External vendor APIs can change. Pin behavior with integration tests before
  scaling beyond pilot traffic.
